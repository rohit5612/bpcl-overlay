# Required on Netlify (baked in at build time). Must match broadcast-api BROADCAST_SECRET.

VITE_BROADCAST_API_ORIGIN=https://broadcast.example.com
VITE_SOCKET_TOKEN=match_BROADCAST_SECRET_from_server_env
# Optional: set to 1 or true for animated draft hero WebMs
# VITE_DRAFT_HERO_ANIMATED=1
`