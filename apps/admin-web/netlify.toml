# Admin Netlify config lives at repo root: ../../netlify.toml
# Netlify only reads netlify.toml from the repository root unless
# "Base directory" is set to apps/admin-web (then use publish = "dist"
# and command = "cd ../.. && npm ci && npm run build:admin").
