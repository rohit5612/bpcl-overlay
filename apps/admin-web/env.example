# Optional defaults when API link is not saved in the browser (Netlify: set in Site env).

VITE_ADMIN_API_ORIGIN=https://broadcast.example.com
# Legacy alias (also read if VITE_ADMIN_API_ORIGIN is unset):
# VITE_BROADCAST_API_ORIGIN=https://broadcast.example.com
